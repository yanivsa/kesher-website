SEO/GEO area reviewed: Canonical routes and sitemap accuracy.

Issue found: The couples counseling Ashdod page was available under two separate routes (`/couples-counseling-ashdod` and `/services/couples/ashdod`), both rendering the same `<CouplesCounselingAshdodPage />`.

Fix applied:
- Removed `/services/couples/ashdod` from the list of static routes in `scripts/content-policy.cjs` and `scripts/generate-sitemap.cjs` so that the duplicated route is removed from the sitemap and `llms-full.txt`.
- Set up a client-side route redirect in `src/App.tsx` from `/services/couples/ashdod` to `/couples-counseling-ashdod`.
- Set up a server-side route redirect in `public/_redirects` from `/services/couples/ashdod` to `/couples-counseling-ashdod`.

Changed files:
- `public/_redirects`
- `public/sitemap.xml`
- `scripts/content-policy.cjs`
- `scripts/generate-sitemap.cjs`
- `src/App.tsx`

Validation command: `npm run check`

Risks/Recommendations: Prerendering / SSG might still be necessary as a larger SEO architecture improvement if search crawlers continue facing issues consuming dynamically mounted single-page app components, though this issue fixes a direct metadata and indexation deduplication problem.

No manual Start/approval was required.
