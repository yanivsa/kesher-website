name: Kesher Content Controller

on:
  push:
    branches: [main]
  workflow_run:
    workflows:
      - Kesher Article Generation
      - Kesher Trusted Article Image
      - Kesher Daily NotebookLM Video Overview
      - Deploy to Cloudflare Pages
    types: [completed]
  schedule:
    # Recovery heartbeat only. Normal successful progress is event-driven.
    - cron: "3,18,33,48 * * * *"
  workflow_dispatch:

permissions:
  actions: write
  contents: write
  pull-requests: read

concurrency:
  group: kesher-content-controller
  cancel-in-progress: false

jobs:
  controller:
    # PR validation runs are irrelevant. Production child failures wake this
    # workflow only to persist a passive wait; actual retry is heartbeat-owned.
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.event != 'pull_request' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout current main
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          ref: main
          persist-credentials: false

      - name: Run autonomous queue-aware reconciliation tick
        env:
          GH_TOKEN: ${{ github.token }}
          GITHUB_TOKEN: ${{ github.token }}
          KESHER_CONTROLLER_STATE_REF: automation-state
          KESHER_TRIGGER_EVENT: ${{ github.event_name }}
          KESHER_CHILD_RUN_ID: ${{ github.event.workflow_run.id }}
          KESHER_CHILD_WORKFLOW: ${{ github.event.workflow_run.name }}
          KESHER_CHILD_CONCLUSION: ${{ github.event.workflow_run.conclusion }}
        run: |
          set -euo pipefail
          python3 -u scripts/kesher_content_controller_v3_best_effort.py --report-json | tee /tmp/kesher-controller.json

      - name: Write concise controller summary
        if: always()
        run: |
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path
          path = Path('/tmp/kesher-controller.json')
          if not path.exists():
              text = 'Controller did not produce a state report.'
          else:
              payload = json.loads(path.read_text(encoding='utf-8'))
              state = payload.get('state') or {}
              article = state.get('article') or {}
              image = state.get('image') or {}
              video = state.get('video') or {}
              last_error = state.get('last_error') or {}
              text = (
                  f"Cycle: {state.get('cycle')}\n\n"
                  f"Status: {state.get('status')}\n\n"
                  f"Action: {payload.get('action')} — {payload.get('reason')}\n\n"
                  f"Article: {article.get('url') or article.get('pr_url') or 'pending'}\n\n"
                  f"Article attempts: {article.get('attempt_count', 0)}/3 | next retry: {article.get('next_retry_at') or 'none'}\n\n"
                  f"Image: {image.get('provider_id') or image.get('status') or 'pending'}\n\n"
                  f"Image attempts: {image.get('attempt_count', 0)}/3 | next retry: {image.get('next_retry_at') or 'none'}\n\n"
                  f"Video: {video.get('youtube_url') or video.get('item_id') or 'pending'}\n\n"
                  f"Video attempts: {video.get('attempt_count', 0)}/3 | next retry: {video.get('next_retry_at') or 'none'}\n\n"
                  f"Last error: {last_error.get('code') or 'none'}"
              )
          with open(os.environ['GITHUB_STEP_SUMMARY'], 'a', encoding='utf-8') as out:
              out.write('### Kesher Content Controller\n\n' + text + '\n')
          PY
